Identify personally identifiable information in the given text such as the following categories: last names, usernames, dates, times, addresses, locations, IDs, emails, and sex. 
Return each entity as separate JSON item. Don't include items for categories not detected. Do not include cateories with values such as "None" or "Null". Respond only with JSON, without any prefixes or suffixes.

Return in the format:
[
    {{"type": category of pii, "value": item identified}}
]

Input: {text}
